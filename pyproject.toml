[project]
name = "rocopilot"
version = "2.0.1"
description = "基于 Interception 的中心点自动丢球工具"
requires-python = ">=3.11"
dependencies = [
    "pywin32>=306",
    "interception-python",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]
